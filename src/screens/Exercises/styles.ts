import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        flex: 1,
        width: '100%',
    },
    textExercise:{
        marginRight: 10
    },
    exerciseContainer:{
        marginTop: 10,
    }
    
})

export default styles